# cvm-windows
script that compiles cvm webapp lollolol

i might make a bettervm version

# which one to use
windows 8.0 and earlier use the win7 version

win10-unsupported is mostly functional, but as the name says it is unsupported
